services:
  - type: web
    name: financial-report-backend
    env: node
    buildCommand: npm install && cd backend && npm install
    startCommand: node backend/server.js
    envVars:
      - key: PORT
        value: 5000
      - key: ADMIN_PASS
        value: work2026@
